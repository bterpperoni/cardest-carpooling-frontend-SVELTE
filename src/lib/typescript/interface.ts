export type coordonates = {
    lat: number,
    long: number
}

export type User = {
    id: number,
    username: string,
    firstName: string,
    familyName: string,
    email: string,
    password: string,
}